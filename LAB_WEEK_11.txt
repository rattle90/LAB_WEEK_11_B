When a user takes a picture, that picture is stored in a path based on the given URI. In which part of the code handles this? (Copy that part of the code as the answer)
Jawab:
Yang menangani hal ini ialah fungsi insertToStore dalam kelas ProviderFileManager.kt. Bagian kodenya adalah sebagai berikut
private fun insertToStore (fileInfo: FileInfo, contentUri: Uri, 
    contentValues: ContentValues) {
    executor.execute {
        val insertedUri = contentResolver.insert(contentUri, contentValues)

        insertedUri?.let {
            val inputStream = contentResolver.openInputStream(fileInfo.uri)
            val outputStream = contentResolver.openOutputStream(insertedUri)
            
            IOUtils.copy(inputStream, outputStream)
        }
    }
}

In your FileInfo.kt, there are 5 attributes. On the first attribute, what does the URI refer to? And on the fourth attribute, what does relativePath refer to?
Jawab:
URI pada atribut pertama mengacu pada FileProvider URI. URI ini digunakan untuk memberi akses sementara dan juga terbatas kepada aplikasi lain (seperti kamera) ke file yang tersimpan di ruang lingkup privat aplikasi kita, memungkinkan aplikasi kamera menyimpan foto/video hasil potret.
Sedangkan, untuk relativePath dalam atribut keempat mengacu pada nama folder standar di MediaStore dimana file akan disimpan.

Explain the chronological order from when a user takes a picture until the file is stored in the MediaStore.
- User menekan tombol "Photo"
- Cek izin
- Membuat file info sementara
- Meluncurkan kamera
- Mengambil gambar
- Penyisipan ke MediaStore
- Penyalinan data
- File tersimpan